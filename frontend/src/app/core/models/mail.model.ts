export interface Mail {

    asunto: string;         // asunto del mensaje a enviar
    destinatario: string;   // email del destinatario del mensaje
    cuerpo: string;        // cuerpo del mensaje de email

}